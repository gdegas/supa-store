type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Item {
  id: ID! @unique
  title: String!
  description: String!
  largeImage: String
  image: String
  # user: User!
  price: Int!
  # createdAt: DateTime!
  # updatedAt: DateTime!
}
